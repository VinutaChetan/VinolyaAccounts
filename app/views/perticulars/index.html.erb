<p id="notice"><%= notice %></p>

<% if current_user.is_admin?%>
  <%= link_to 'New Particular', new_perticular_path,class: "btn btn-success" %>
<%end%><br/>
<br/>
<h2 class="text-muted">Listing Particulars</h2>
<br/>

<br/>
<%if @perticulars.empty? %>
  <p>No perticulars found </p>
<%else %>
<div class="container">
<table class ="table table-striped" id="myTable">
  <thead>
    <tr>
      <th class="text-success">Name</th>
      <th class="text-success">Perticular Type</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @perticulars.order('created_at DESC').each do |perticular| %>
      <tr>
        <td><%= perticular.try(:name) %></td>
        <td><%= perticular.try(:perticular_type) %></td>
        <td><%= link_to 'Show', perticular,class: "btn btn-info" %></td>
        <% if can? :update,perticular %> 
          <td><%= link_to 'Edit', edit_perticular_path(perticular),class: "btn btn-info" %></td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<%end%>
<br>

<script type="text/javascript">
   $('#myTable').DataTable();  
</script>

